export const NAVME_TABLES = {
  logins: 'navme_logins',
  pois: 'navme_pois',
  /** Per-project 2D floor editor JSON (walk grid, zones, slice Y) — not navme_logins. */
  floorEdits: 'navme_floor_edits',
} as const;
